import styled from '@emotion/styled';
import { Button } from '@mui/material';
import { Box } from '@mui/system';

export const StyledBox = styled(Box)`
  position: absolute;
  right: 5px;
  bottom: 7px;
`;

export const StyledButtonDiscard = styled(Button)`
  margin-right: 5px;
`;
