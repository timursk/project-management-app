import styled from '@emotion/styled';

const StyledTaskCardControlsWrapper = styled('div')(() => ({
  display: 'flex',
  justifyContent: 'flex-end',
}));

export default StyledTaskCardControlsWrapper;
