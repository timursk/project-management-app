import InitialState from '../types/store/initialState';

export const initialState: InitialState = {
  user: {
    name: null,
    login: null,
  },
  boards: null,
  columns: null,
  tasks: null,
};
